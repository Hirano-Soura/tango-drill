# タスク

**進捗を書いてよいのはこの文書だけ**(UD-3)。記号は共通ルール §6 の定義表に従う。
時間見積は書かない(UD-5)。各行は完了条件を持つ(US-6)。

## 第 1 段階

| ID | 状態 | 内容 | 完了条件 |
| --- | --- | --- | --- |
| T-0 | ✅ | リポジトリ立ち上げ | 立ち上げチェックリストの全項目が揃い、`check_env.py` のレポートが PASS |
| T-0.1 | 🆕 ✅ | GitHub への公開と CI | リモートへ push し、GitHub Actions の 3 系統がすべて通る |
| T-1 | ✅ | `core/`: 取り込み形式の解析(簡易形式・JSON)と仕様書 `Docs/20_ImportFormat.md` | 見本と、壊した見本(陽性対照)を含むテストが `npm test` で通る。INV-2 / INV-3 のテストがある |
| T-2 | ✅ | `core/`: 誤答の選定 | 登録語数 0 / 1〜3 / 4〜(仮)9 / (仮)10 以上 × 内蔵語彙の有無の全段がテストで再現される。INV-5 の陽性対照がある |
| T-2.1 | 🆕 ✅ | 内蔵語彙のデータを同梱する(T-2 で判明。持つ項目は [21_Quiz.md](21_Quiz.md) §4) | 同梱した内蔵語彙が 495 語(toeic-drill の単語データを鍵で重複を除いた数。2026-09-24 に数えた値)で、どの語も §4 の項目だけを持つことをテストで示す |
| T-3 | ✅ | `core/`: 復習ミックスと集計 | 既存アプリと同じ入力で同じ出題集合が出ることを比較テストで示す。出題集合を `notOwnQuestions`(INV-5)に通して空であることもテストで示す |
| T-4 | ✅ | 保存層(IndexedDB)とバックアップ。2026-09-24 に内蔵ブラウザで確認([22_Storage.md](22_Storage.md) §5 の手順) | 書き出した中身を別の IndexedDB(別オリジン)で読み込み、語数と記録件数が一致する。元に戻す・リロード後の保持も確かめる。INV-1 の機械検査が入る。ファイルの受け渡しと別製品のブラウザでの確認は T-5.2 に移した(当初の条件は「書き出したファイルを別ブラウザで読み込み」。ファイルを渡すには画面が要るため) |
| T-4.1 | 🆕 ✅ | 保存層: 取り込みで品詞が埋まり鍵が変わった語の記録を付け替える(`applyImport` の `rekeys`。T-1 で判明) | 品詞の無い語に記録を付けてから品詞を埋める取り込みを行い、記録が新しい鍵に付いていることをテストで示す |
| T-5 | ✅ | 画面 5 タブ。2026-09-24 に `npm run e2e` で PC 幅とスマホ幅の両方が通った([23_Screens.md](23_Screens.md)) | Playwright で「追加 → 学習 → 記録 → 編集 → 削除 → 元に戻す」を通す。編集で例文を消すと和訳と出どころも消える([20_ImportFormat.md](20_ImportFormat.md) §4 の前提) |
| T-5.1 | 🆕 ✅ | 画面: 取り込みの確認表(INV-2 の画面側。T-1 で判明)。2026-09-25 に `npm run e2e` で PC 幅とスマホ幅の両方が通った(`tests/e2e/import.spec.js`。陽性対照は [23_Screens.md](23_Screens.md) §6) | 貼り付け → 確認表 → 確定 の順を経ないと単語帳が変わらないことを Playwright で示す |
| T-5.2 | 🆕 ⬜ | 画面: バックアップの書き出し・読み込み(件数を見せる確認と元に戻す。T-4 で判明)。画面は設定タブに作り、2026-09-25 に `npm run e2e` で PC 幅とスマホ幅の両方が通った(Chromium。書き出したファイルを別のブラウザコンテキストで読み込む。`tests/e2e/backup.spec.js`)。**残りは iOS Safari の実機での確認** | Chrome で書き出したファイルを iOS Safari で読み込み、確認に出る語数・記録件数が書き出し元と一致する。元に戻すと読み込む前の単語帳に戻る |
| T-6 | ⬜ | AI との往復 | ChatGPT・Claude・Gemini それぞれの実際の返答を貼って取り込め、その返答が見本としてテストに入っている |
| T-7 | ⬜ | PWA 化 | iOS Safari と Android Chrome でホーム画面に追加し、機内モードで学習できる |
| T-8 | ⬜ | 既存の TOEIC 単語 495 項目の移行(自分でも日常的に使う) | 取り込み後の語数が 495 で、既存アプリの記録と突き合わせて欠落が無い |
| T-9 | ⬜ | 先生との試用 | 先生と数名の受講者で使い、出た要望を派生タスクとして追記する(US-4) |

## 未決定事項・未検証項目

| 状態 | 事項 | 判断材料・確認方法 |
| --- | --- | --- |
| ◇ | 共通ルールが `CLAUDE.local.md` 経由で読み込まれるか(未検証) | 各端末でセッションの冒頭に「`US-1` は何か」と聞き、答えられることを確かめる。laptop の T-1 のセッションでは、`CLAUDE.local.md` の import 行は見えたが**共通ルールの本文は文脈に入っていなかった**(本文を Read で読んで補った)。リポジトリの外のファイルを import するには初回の承認が要る可能性がある。対話できるターミナルの `claude` で開き直し、承認を求められるか確かめる。クラウドのセッション(claude.ai/code)では clone 直後に `CLAUDE.local.md` が無く段 1 が FAIL する。共通ルールは Vault のリポジトリにあるので、それを同じセッションに加えて `CLAUDE.local.md` を作れば段 1 は PASS するが、作る前に始まったセッションの文脈には入らない(本文を Read で読んで補う)。desktop でも T-4 のセッションの途中で作ったので、次のセッションで確かめる。2026-09-25 の laptop のセッション(Claude デスクトップアプリの Code タブ)でも、import 行は見えたが本文は文脈に入っていなかった(Read で補った)。同じ日のクラウドのセッションでは、Vault(`Knowledge-`)をセッションに加える操作が自動の権限判定で一度拒否され、本人が許可してから加えられた。クラウドで始めるときは、最初に Vault を加えてよいかを本人に確かめる |
| ◇ | 既存の単語データの移行(T-8)で、既存の回を追加日に引き継ぐか | 回は追加日で束ねる([21_Quiz.md](21_Quiz.md) §5)ので、そのまま取り込むと 495 語が取り込んだ日の 1 回になる。既存の単語データは日ごとのファイルで、日付を持つ。取り込みで追加日を渡せるようにするかを T-8 で決める |
| ◇ | 取り込み形式の仮の値(仮決定済み) | 区切りの無い行を見出し語とみなす語数・見出し語の大文字小文字・見出し行の項目名・品詞とみなす値。[20_ImportFormat.md](20_ImportFormat.md) §5。T-6 の実際の AI の返答で見直す |
| ◇ | 出題の仮の値(仮決定済み) | 自分の語だけで誤答を作る境界の語数。[21_Quiz.md](21_Quiz.md) §3 |
| ◇ | `.gitattributes`(改行コードの固定) | 共有設定のため提案にとどめている(UT-2)。laptop と desktop の両方で編集するので、入れることを提案する |
| ◇ | 画面の実機での目視(未検証) | T-5 の確認は Playwright(PC の Edge。スマホ幅は画面の幅を変えただけ)まで。iOS Safari・Android Chrome の実機での表示と操作(ソフトウェアキーボードで入力欄が隠れないか等)は、T-7 で実機に載せるときに目視する。T-5.2 のバックアップは、iOS Safari でファイルを選べるか(`accept` の指定で JSON のファイルが選べなくならないか)と、書き出したファイルが端末のどこに保存されるかも見る |
| ◇ | ライセンスの種類 | 先生・大学への引き継ぎを想定して決める |
| ◇ | GitHub Pages で公開する時期 | 画面(T-5)と取り込み(T-5.1)はできた。公開して授業で案内する前に、先生の了承を取る。バックアップ(T-5.2)・PWA(T-7)を待つかも、そのときに決める |
| ◇ | 同じ入力の中に、同じ綴りの品詞ありの行と品詞なしの行があるとき(例: 品詞が「名」の `itinerary` の行と、品詞の無い `itinerary` の行) | 今の規則([20_ImportFormat.md](20_ImportFormat.md) §4)では鍵が違うので重複にならず、単語帳に同じ綴りの語が無ければ 2 語とも「足す」になる(2026-09-25 に画面で確認)。確認表で片方を外せる。実際の AI の返答(T-6)でこの形が出るなら、品詞なしの行を品詞ありの行に寄せるかを決める |
| ◇ | 時間見積の機械検査(UD-5) | `doc_audit.py` に未導入。誤検出の少ない検出条件が決まったら WARN で足す |
